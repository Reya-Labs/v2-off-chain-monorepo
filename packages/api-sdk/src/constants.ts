// TODO: Costin API url should be without a / at end
// this makes the weird templating ${url}${controller}
// become ${url}/${controller}, which is more natural
export const API_URL = 'https://api.voltz.xyz/';
