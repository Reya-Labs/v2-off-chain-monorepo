import { MINUS_ONE_BN, ZERO_BN } from '../constants';

export const CRITICAL_ERROR_MESSAGE =
  'Phew, that was unexpected. Please, reach out to the community for support via Discord!';

export const DEFAULT_EXECUTED_QUOTE = ZERO_BN;
export const DEFAULT_EXECUTED_BASE = ZERO_BN;
export const DEFAULT_FEE = MINUS_ONE_BN;
export const DEFAULT_TICK = -1;
