import { GetMaxLeverageArgs } from './types';

export const getMaxLeverage = async (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _: GetMaxLeverageArgs,
): Promise<number> => {
  return 1;
};
