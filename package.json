{
  "name": "v2-off-chain-monorepo",
  "version": "1.0.0",
  "private": true,
  "repository": "https://github.com/Voltz-Protocol/v2-indexer.git",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "yarn workspace api run lint && yarn workspace indexer run lint && yarn workspace utils run lint",
    "format": "yarn workspace api run format && yarn workspace indexer run format && yarn workspace utils run format",
    "build": "yarn workspace api run build && yarn workspace indexer run build && yarn workspace utils run build",
    "test": "yarn workspace api run test && yarn workspace indexer run test && yarn workspace utils run test"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@typescript-eslint/eslint-plugin": "^5.59.2",
    "@typescript-eslint/parser": "^5.59.2",
    "eslint": "^8.39.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "jest": "^29.5.0",
    "prettier": "^2.8.8"
  },
  "packageManager": "yarn@3.5.1"
}
